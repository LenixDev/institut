ntre un chifre: ";
        cin >> n;
        if (n) {
            n *= n;
        };
        cout << "La resultat est: " << n << "\n";
        cout << "voulez vous continue? Entre 'Y' pour oui, et 'N' pour Non: ";
        cin >> repon